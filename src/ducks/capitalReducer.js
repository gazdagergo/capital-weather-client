const initialState = {
  savedCities: [
    { id: 178, capitalName: 'Budapest', countryCode: 'HU' }
  ]
};

const capitalReducer = (state = initialState, action) => {
  return state;
}

export default capitalReducer;
